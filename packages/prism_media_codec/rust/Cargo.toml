[package]
name = "prism_media_codec_ffi"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "prism_media_codec_ffi"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
flutter_rust_bridge = "=2.12.0"
image = { version = "0.25", default-features = false, features = ["gif", "jpeg", "png", "webp"] }
