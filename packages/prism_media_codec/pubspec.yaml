name: prism_media_codec
description: App-owned native image normalization helpers for Prism.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.11.1

dependencies:
  code_assets: ^1.2.1
  flutter_rust_bridge: 2.12.0
  hooks: ^2.0.2
  meta: ^1.11.0
  native_toolchain_rust: ^1.0.4+0

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.26.0
