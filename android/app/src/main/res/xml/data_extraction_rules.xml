<?xml version="1.0" encoding="utf-8"?>
<!--
  Android Data Extraction Rules (API 31+).
  Covers both cloud backup and device-to-device transfer.
  https://developer.android.com/guide/topics/data/autobackup#define-device-conditions-for-inclusion
-->
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude all app databases -->
        <exclude domain="database" path="." />
        <!-- Exclude shared prefs (may contain sensitive flags) -->
        <exclude domain="sharedpref" path="." />
        <!-- Exclude exported .prism backup files -->
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <!-- Exclude all app databases from device-to-device transfer -->
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
